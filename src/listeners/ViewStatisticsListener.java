package listeners;

public interface ViewStatisticsListener {
	/**
	 * Implemented by the controller and added to the view. Called when the user
	 * wishes to view persistent statistics.
	 */
	public void viewStatistics();
}
