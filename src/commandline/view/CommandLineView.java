package commandline.view;

import model.Card;
import model.RequestDecision;

public class CommandLineView implements RequestDecision {

	@Override
	public int makeDecision(Card card) {
		// TODO display card to user and get back a response (card property index).
		return 0;
	}

}
